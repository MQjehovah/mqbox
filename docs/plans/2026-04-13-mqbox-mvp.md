# MQBox MVP Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** 构建一个跨平台个人效率工具助手，通过全局快捷键呼出搜索框，支持文件搜索和插件系统。

**Architecture:** Electron主进程负责全局快捷键、文件搜索、插件管理；Vue 3渲染进程负责UI展示；通过IPC桥接通信。

**Tech Stack:** Electron 28+, Vue 3, TypeScript, Vite, Pinia, UnoCSS, Element Plus, uiohook-napi, lowdb

---

## Task 1: 项目初始化

**Files:**
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `vite.config.ts`
- Create: `electron-builder.yml`

**Step 1: 创建 package.json**

```json
{
  "name": "mqbox",
  "version": "1.0.0",
  "description": "个人效率工具助手",
  "main": "dist-electron/main/index.js",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build && electron-builder",
    "preview": "vite preview",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "pinia": "^2.1.0",
    "element-plus": "^2.5.0",
    "lowdb": "^7.0.0",
    "chokidar": "^3.5.0",
    "uiohook-napi": "^1.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vue-tsc": "^1.8.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "electron": "^28.0.0",
    "electron-builder": "^24.0.0",
    "unocss": "^0.58.0"
  }
}
```

**Step 2: 创建 tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "paths": {
      "@/*": ["./src/renderer/src/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.vue"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

**Step 3: 创建 tsconfig.node.json**

```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true
  },
  "include": ["vite.config.ts", "src/main/**/*.ts", "src/preload/**/*.ts"]
}
```

**Step 4: 创建 vite.config.ts**

```ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import electron from 'vite-plugin-electron'
import UnoCSS from 'unocss/vite'
import { resolve } from 'path'

export default defineConfig({
  plugins: [
    vue(),
    UnoCSS(),
    electron([
      {
        entry: 'src/main/index.ts',
        onstart(options) { options.reload() },
        vite: { build: { outDir: 'dist-electron/main' } }
      },
      {
        entry: 'src/preload/index.ts',
        onstart(options) { options.reload() },
        vite: { build: { outDir: 'dist-electron/preload' } }
      }
    ])
  ],
  resolve: { alias: { '@': resolve(__dirname, 'src/renderer/src') } },
  build: { outDir: 'dist' }
})
```

**Step 5: 创建 electron-builder.yml**

```yaml
appId: com.mqbox.app
productName: MQBox
directories:
  output: release
files:
  - dist/**/*
  - dist-electron/**/*
  - package.json
win:
  icon: resources/icon.png
  target: nsis
nsis:
  oneClick: false
  perMachine: true
mac:
  icon: resources/icon.png
  target: dmg
linux:
  icon: resources/icon.png
  target: deb
```

**Step 6: 安装依赖**

Run: `npm install`
Expected: 依赖安装成功

**Step 7: Commit**

```bash
git add package.json tsconfig.json tsconfig.node.json vite.config.ts electron-builder.yml
git commit -m "feat: 项目初始化配置"
```

---

## Task 2: 主进程入口

**Files:**
- Create: `src/main/index.ts`

**Step 1: 创建主进程入口**

```ts
import { app, BrowserWindow, globalShortcut, ipcMain } from 'electron'
import { join } from 'path'
import { setupShortcut } from './shortcut'
import { setupTray } from './tray'
import { setupIPC } from './ipc'

let mainWindow: BrowserWindow | null = null

function createWindow() {
  mainWindow = new BrowserWindow({
    width: 680,
    height: 500,
    frame: false,
    transparent: true,
    alwaysOnTop: true,
    skipTaskbar: true,
    resizable: false,
    webPreferences: {
      preload: join(__dirname, '../preload/index.js'),
      nodeIntegration: false,
      contextIsolation: true
    }
  })

  if (process.env.NODE_ENV === 'development') {
    mainWindow.loadURL('http://localhost:5173')
    mainWindow.webContents.openDevTools()
  } else {
    mainWindow.loadFile(join(__dirname, '../../dist/index.html'))
  }

  mainWindow.on('blur', () => {
    mainWindow?.hide()
  })
}

app.whenReady().then(() => {
  createWindow()
  setupShortcut(mainWindow!)
  setupTray(mainWindow!)
  setupIPC()
})

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') app.quit()
})

app.on('will-quit', () => {
  globalShortcut.unregisterAll()
})

export { mainWindow }
```

**Step 2: Commit**

```bash
git add src/main/index.ts
git commit -m "feat: 主进程入口"
```

---

## Task 3: 快捷键管理

**Files:**
- Create: `src/main/shortcut.ts`

**Step 1: 创建快捷键模块**

```ts
import { globalShortcut, BrowserWindow, app } from 'electron'
import { getConfig } from './config'

export function setupShortcut(win: BrowserWindow) {
  const config = getConfig()
  const toggleKey = config.shortcut?.toggle || 'CommandOrControl+Space'

  const ret = globalShortcut.register(toggleKey, () => {
    if (win.isVisible()) {
      win.hide()
    } else {
      win.show()
      win.focus()
    }
  })

  if (!ret) {
    console.error(`快捷键 ${toggleKey} 注册失败`)
  }

  console.log(`快捷键 ${toggleKey} 注册成功`)
}
```

**Step 2: Commit**

```bash
git add src/main/shortcut.ts
git commit -m "feat: 全局快捷键管理"
```

---

## Task 4: 托盘管理

**Files:**
- Create: `src/main/tray.ts`
- Create: `resources/tray-icon.png` (占位)

**Step 1: 创建托盘模块**

```ts
import { Tray, Menu, BrowserWindow, app, nativeImage } from 'electron'
import { join } from 'path'

export function setupTray(win: BrowserWindow) {
  const iconPath = join(__dirname, '../../resources/tray-icon.png')
  const icon = nativeImage.createFromPath(iconPath)
  
  const tray = new Tray(icon.resize({ width: 16, height: 16 }))
  
  const contextMenu = Menu.buildFromTemplate([
    { label: '显示', click: () => { win.show(); win.focus() } },
    { label: '插件管理', click: () => { win.webContents.send('show-plugin-manager') } },
    { type: 'separator' },
    { label: '设置', click: () => { win.webContents.send('show-settings') } },
    { type: 'separator' },
    { label: '退出', click: () => app.quit() }
  ])
  
  tray.setToolTip('MQBox')
  tray.setContextMenu(contextMenu)
  
  tray.on('click', () => {
    win.isVisible() ? win.hide() : win.show()
  })
}
```

**Step 2: Commit**

```bash
git add src/main/tray.ts
git commit -m "feat: 系统托盘管理"
```

---

## Task 5: 配置管理

**Files:**
- Create: `src/main/config.ts`

**Step 1: 创建配置模块**

```ts
import { app } from 'electron'
import { join } from 'path'
import { Low, JSONFile } from 'lowdb'
import type { AppConfig } from '../shared/types'

const configPath = join(app.getPath('userData'), 'config.json')
const adapter = new JSONFile<AppConfig>(configPath)
const db = new Low<AppConfig>(adapter, getDefaultConfig())

interface AppConfig {
  version: string
  shortcut: { toggle: string; confirm: string; next: string; prev: string }
  window: { width: number; opacity: number; alwaysOnTop: boolean; hideOnBlur: boolean }
  search: { defaultEngine: string; maxResults: number; enableHistory: boolean }
  fileSearch: { enableEverything: boolean; everythingPort: number; watchDirs: string[] }
  theme: string
  accentColor: string
}

function getDefaultConfig(): AppConfig {
  return {
    version: '1.0.0',
    shortcut: { toggle: 'CommandOrControl+Space', confirm: 'Enter', next: 'Down', prev: 'Up' },
    window: { width: 680, opacity: 0.95, alwaysOnTop: true, hideOnBlur: true },
    search: { defaultEngine: 'file', maxResults: 10, enableHistory: true },
    fileSearch: { enableEverything: true, everythingPort: 8081, watchDirs: [] },
    theme: 'auto',
    accentColor: '#0078D4'
  }
}

export async function initConfig() {
  await db.read()
  if (!db.data) db.data = getDefaultConfig()
  await db.write()
}

export function getConfig(): AppConfig {
  return db.data
}

export async function setConfig(key: string, value: any) {
  const keys = key.split('.')
  let obj = db.data as any
  for (let i = 0; i < keys.length - 1; i++) obj = obj[keys[i]]
  obj[keys[keys.length - 1]] = value
  await db.write()
}
```

**Step 2: Commit**

```bash
git add src/main/config.ts
git commit -m "feat: 配置文件管理"
```

---

## Task 6: IPC通信层

**Files:**
- Create: `src/main/ipc/index.ts`
- Create: `src/shared/types.ts`

**Step 1: 创建共享类型定义**

```ts
export interface SearchResult {
  type: 'file' | 'plugin' | 'command'
  title: string
  subtitle?: string
  icon?: string
  action: string
  actionArgs?: any
}

export interface FileResult {
  path: string
  name: string
  extension: string
  size: number
  modifiedTime: number
}

export interface PluginInfo {
  id: string
  name: string
  version: string
  description: string
  icon?: string
  enabled: boolean
  keywords: string[]
  permissions: string[]
}

export interface QuickPickItem {
  id: string
  label: string
  description?: string
  detail?: string
}
```

**Step 2: 创建 IPC 处理**

```ts
import { ipcMain, clipboard, BrowserWindow, dialog, shell } from 'electron'
import { searchFiles } from '../search/everything'
import { listPlugins, enablePlugin, disablePlugin, executePlugin } from '../plugin/host'
import { getConfig, setConfig } from '../config'

export function setupIPC() {
  // 搜索相关
  ipcMain.handle('search:query', async (_, query: string) => {
    return await searchFiles(query)
  })

  // 插件相关
  ipcMain.handle('plugin:list', async () => listPlugins())
  ipcMain.handle('plugin:enable', async (_, id: string) => enablePlugin(id))
  ipcMain.handle('plugin:disable', async (_, id: string) => disablePlugin(id))
  ipcMain.handle('plugin:execute', async (_, id: string, action: string, args: any) => 
    executePlugin(id, action, args))

  // 配置相关
  ipcMain.handle('config:get', async (_, key?: string) => 
    key ? getConfig()[key as any] : getConfig())
  ipcMain.handle('config:set', async (_, key: string, value: any) => setConfig(key, value))

  // 窗口相关
  ipcMain.on('window:show', () => BrowserWindow.getAllWindows()[0]?.show())
  ipcMain.on('window:hide', () => BrowserWindow.getAllWindows()[0]?.hide())

  // 剪贴板相关
  ipcMain.handle('clipboard:read', async () => clipboard.readText())
  ipcMain.handle('clipboard:write', async (_, text: string) => clipboard.writeText(text))

  // 文件操作
  ipcMain.handle('file:open', async (_, path: string) => shell.openPath(path))
  ipcMain.handle('file:showInExplorer', async (_, path: string) => shell.showItemInFolder(path))
}
```

**Step 3: Commit**

```bash
git add src/main/ipc/index.ts src/shared/types.ts
git commit -m "feat: IPC通信层"
```

---

## Task 7: Everything文件搜索集成

**Files:**
- Create: `src/main/search/everything.ts`

**Step 1: 创建 Everything 搜索模块**

```ts
import { getConfig } from '../config'
import type { FileResult } from '../../shared/types'

export async function searchFiles(query: string): Promise<FileResult[]> {
  const config = getConfig()
  
  if (!config.fileSearch.enableEverything) {
    return [] // 后续实现自建索引
  }

  try {
    const port = config.fileSearch.everythingPort
    const response = await fetch(`http://127.0.0.1:${port}/?search=${encodeURIComponent(query)}&json=1`)
    const data = await response.json()
    
    return data.results?.map((item: any) => ({
      path: item.path,
      name: item.name,
      extension: item.type === 'folder' ? '' : item.name.split('.').pop() || '',
      size: item.size || 0,
      modifiedTime: item.date_modified || 0
    })) || []
  } catch (error) {
    console.error('Everything搜索失败:', error)
    return []
  }
}
```

**Step 2: Commit**

```bash
git add src/main/search/everything.ts
git commit -m "feat: Everything文件搜索集成"
```

---

## Task 8: 插件系统核心

**Files:**
- Create: `src/main/plugin/loader.ts`
- Create: `src/main/plugin/host.ts`
- Create: `src/main/plugin/sandbox.ts`

**Step 1: 创建插件加载器**

```ts
import { app } from 'electron'
import { join } from 'path'
import { existsSync, readdirSync, readFileSync } from 'fs'
import type { PluginInfo } from '../../shared/types'

const pluginsDir = join(app.getPath('userData'), 'plugins')

interface PluginManifest {
  name: string
  version: string
  displayName: string
  description: string
  main: string
  icon?: string
  keywords: string[]
  activationEvents: string[]
  permissions: string[]
}

export function loadPlugins(): Map<string, { manifest: PluginManifest; module: any }> {
  const plugins = new Map()
  
  if (!existsSync(pluginsDir)) return plugins
  
  const dirs = readdirSync(pluginsDir, { withFileTypes: true })
    .filter(d => d.isDirectory())
  
  for (const dir of dirs) {
    const pluginPath = join(pluginsDir, dir.name)
    const manifestPath = join(pluginPath, 'package.json')
    
    if (!existsSync(manifestPath)) continue
    
    try {
      const manifest: PluginManifest = JSON.parse(readFileSync(manifestPath, 'utf-8'))
      const indexPath = join(pluginPath, manifest.main)
      
      if (existsSync(indexPath)) {
        const module = require(indexPath)
        plugins.set(dir.name, { manifest, module })
      }
    } catch (error) {
      console.error(`加载插件 ${dir.name} 失败:`, error)
    }
  }
  
  return plugins
}

export function getPluginInfo(id: string, manifest: PluginManifest): PluginInfo {
  return {
    id,
    name: manifest.displayName,
    version: manifest.version,
    description: manifest.description,
    icon: manifest.icon,
    enabled: true,
    keywords: manifest.keywords,
    permissions: manifest.permissions
  }
}
```

**Step 2: 创建插件宿主**

```ts
import { loadPlugins, getPluginInfo } from './loader'
import { createSandbox } from './sandbox'
import type { PluginInfo } from '../../shared/types'

const loadedPlugins = loadPlugins()
const activePlugins = new Map<string, any>()
const pluginContexts = new Map<string, any>()

export function listPlugins(): PluginInfo[] {
  return Array.from(loadedPlugins.entries()).map(([id, { manifest }]) => 
    getPluginInfo(id, manifest))
}

export function enablePlugin(id: string): boolean {
  const plugin = loadedPlugins.get(id)
  if (!plugin) return false
  
  const sandbox = createSandbox(plugin.manifest.permissions)
  const context = {
    registerCommand: (name: string, handler: Function) => {
      sandbox.commands.set(name, handler)
    },
    registerSearchProvider: (provider: any) => {
      sandbox.searchProviders.set(provider.keyword, provider)
    },
    clipboard: sandbox.api.clipboard,
    files: sandbox.api.files,
    ui: sandbox.api.ui,
    storage: sandbox.api.storage,
    notification: sandbox.api.notification,
    shell: sandbox.api.shell
  }
  
  plugin.module.activate(context)
  activePlugins.set(id, sandbox)
  pluginContexts.set(id, context)
  
  return true
}

export function disablePlugin(id: string): boolean {
  const plugin = loadedPlugins.get(id)
  if (!plugin) return false
  
  plugin.module.deactivate?.()
  activePlugins.delete(id)
  pluginContexts.delete(id)
  
  return true
}

export async function executePlugin(id: string, action: string, args: any): Promise<any> {
  const sandbox = activePlugins.get(id)
  if (!sandbox) return null
  
  const handler = sandbox.commands.get(action)
  if (handler) return handler(args)
  
  return null
}

export function getSearchProviders(): Map<string, any> {
  const allProviders = new Map()
  for (const [_, sandbox] of activePlugins) {
    for (const [keyword, provider] of sandbox.searchProviders) {
      allProviders.set(keyword, provider)
    }
  }
  return allProviders
}
```

**Step 3: 创建插件沙箱**

```ts
import { clipboard, shell, Notification } from 'electron'
import { app } from 'electron'
import { join } from 'path'
import { Low, JSONFile } from 'lowdb'

export function createSandbox(permissions: string[]) {
  const commands = new Map<string, Function>()
  const searchProviders = new Map<string, any>()
  
  const api = {
    clipboard: hasPermission('clipboard') ? {
      readText: async () => clipboard.readText(),
      writeText: async (text: string) => clipboard.writeText(text)
    } : null,
    
    files: hasPermission('files:read') || hasPermission('files:write') ? {
      read: async (path: string) => require('fs').promises.readFile(path, 'utf-8'),
      write: async (path: string, content: string) => require('fs').promises.writeFile(path, content),
      exists: async (path: string) => require('fs').existsSync(path),
      showInExplorer: (path: string) => shell.showItemInFolder(path)
    } : null,
    
    ui: {
      showMessage: (message: string, type?: string) => new Notification({ title: type || 'info', body: message }).show(),
      showInputBox: async (options: any) => null, // 通过 IPC 实现
      showQuickPick: async (items: any[]) => null
    },
    
    storage: {
      get: async (key: string) => null,
      set: async (key: string, value: any) => null
    },
    
    notification: hasPermission('notification') ? {
      show: (title: string, body: string) => new Notification({ title, body }).show()
    } : null,
    
    shell: hasPermission('shell') ? {
      execute: async (command: string) => null,
      openExternal: (url: string) => shell.openExternal(url)
    } : null
  }
  
  function hasPermission(name: string): boolean {
    return permissions.includes(name) || permissions.includes('system')
  }
  
  return { commands, searchProviders, api }
}
```

**Step 4: Commit**

```bash
git add src/main/plugin/loader.ts src/main/plugin/host.ts src/main/plugin/sandbox.ts
git commit -m "feat: 插件系统核心"
```

---

## Task 9: Preload脚本

**Files:**
- Create: `src/preload/index.ts`

**Step 1: 创建 preload 脚本**

```ts
import { contextBridge, ipcRenderer } from 'electron'

const api = {
  search: {
    query: (query: string) => ipcRenderer.invoke('search:query', query)
  },
  plugin: {
    list: () => ipcRenderer.invoke('plugin:list'),
    enable: (id: string) => ipcRenderer.invoke('plugin:enable', id),
    disable: (id: string) => ipcRenderer.invoke('plugin:disable', id),
    execute: (id: string, action: string, args: any) => 
      ipcRenderer.invoke('plugin:execute', id, action, args)
  },
  config: {
    get: (key?: string) => ipcRenderer.invoke('config:get', key),
    set: (key: string, value: any) => ipcRenderer.invoke('config:set', key, value)
  },
  window: {
    show: () => ipcRenderer.send('window:show'),
    hide: () => ipcRenderer.send('window:hide')
  },
  clipboard: {
    read: () => ipcRenderer.invoke('clipboard:read'),
    write: (text: string) => ipcRenderer.invoke('clipboard:write', text)
  },
  file: {
    open: (path: string) => ipcRenderer.invoke('file:open', path),
    showInExplorer: (path: string) => ipcRenderer.invoke('file:showInExplorer', path)
  },
  on: (channel: string, callback: Function) => {
    ipcRenderer.on(channel, (_, ...args) => callback(...args))
  }
}

contextBridge.exposeInMainWorld('mqbox', api)
```

**Step 2: Commit**

```bash
git add src/preload/index.ts
git commit -m "feat: Preload脚本"
```

---

## Task 10: 渲染进程入口

**Files:**
- Create: `src/renderer/index.html`
- Create: `src/renderer/src/main.ts`
- Create: `src/renderer/src/App.vue`

**Step 1: 创建 HTML入口**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MQBox</title>
</head>
<body>
  <div id="app"></div>
  <script type="module" src="/src/main.ts"></script>
</body>
</html>
```

**Step 2: 创建 main.ts**

```ts
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import 'virtual:uno.css'
import App from './App.vue'
import './styles/global.css'

const app = createApp(App)
app.use(createPinia())
app.use(ElementPlus)
app.mount('#app')
```

**Step 3: 创建 App.vue**

```vue
<script setup lang="ts">
import { ref } from 'vue'
import SearchBox from './components/SearchBox.vue'
import PluginManager from './components/PluginManager.vue'
import PluginConfig from './components/PluginConfig.vue'

const currentView = ref('search')

window.mqbox.on('show-plugin-manager', () => currentView.value = 'plugin-manager')
window.mqbox.on('show-settings', () => currentView.value = 'settings')
</script>

<template>
  <div class="app-container">
    <SearchBox v-if="currentView === 'search'" />
    <PluginManager v-if="currentView === 'plugin-manager'" @close="currentView = 'search'" />
    <PluginConfig v-if="currentView === 'plugin-config'" @close="currentView = 'plugin-manager'" />
  </div>
</template>

<style>
.app-container {
  width: 100%;
  height: 100%;
  background: transparent;
}
</style>
```

**Step 4: Commit**

```bash
git add src/renderer/index.html src/renderer/src/main.ts src/renderer/src/App.vue
git commit -m "feat: 渲染进程入口"
```

---

## Task 11: 全局样式和UnoCSS配置

**Files:**
- Create: `src/renderer/src/styles/global.css`
- Create: `uno.config.ts`

**Step 1: 创建全局样式**

```css
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body, #app {
  width: 100%;
  height: 100%;
  background: transparent;
  font-family: 'Segoe UI', system-ui, sans-serif;
  overflow: hidden;
}

::-webkit-scrollbar {
  width: 6px;
}

::-webkit-scrollbar-thumb {
  background: rgba(0, 0, 0, 0.2);
  border-radius: 3px;
}

::-webkit-scrollbar-track {
  background: transparent;
}
```

**Step 2: 创建 UnoCSS 配置**

```ts
import { defineConfig, presetUno, presetAttributify } from 'unocss'

export default defineConfig({
  presets: [presetUno(), presetAttributify()],
  shortcuts: {
    'flex-center': 'flex justify-center items-center',
    'flex-between': 'flex justify-between items-center',
    'text-primary': 'text-[#1E1E1E]',
    'text-secondary': 'text-[#666666]',
    'text-muted': 'text-[#999999]',
    'bg-primary': 'bg-[#0078D4]',
    'bg-card': 'bg-[#F5F5F5]',
    'border-light': 'border-[#E0E0E0]'
  },
  theme: {
    colors: {
      primary: '#0078D4',
      secondary: '#666666',
      muted: '#999999',
      card: '#F5F5F5',
      border: '#E0E0E0'
    }
  }
})
```

**Step 3: Commit**

```bash
git add src/renderer/src/styles/global.css uno.config.ts
git commit -m "feat: 全局样式和UnoCSS配置"
```

---

## Task 12: 搜索框组件

**Files:**
- Create: `src/renderer/src/components/SearchBox.vue`
- Create: `src/renderer/src/stores/search.ts`

**Step 1: 创建搜索状态 Store**

```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import type { SearchResult, FileResult } from '../../../shared/types'

export const useSearchStore = defineStore('search', () => {
  const query = ref('')
  const results = ref<SearchResult[]>([])
  const selectedIndex = ref(0)
  const isLoading = ref(false)

  async function performSearch(q: string) {
    query.value = q
    if (!q.trim()) {
      results.value = []
      return
    }

    isLoading.value = true
    try {
      const files: FileResult[] = await window.mqbox.search.query(q)
      results.value = files.map(f => ({
        type: 'file',
        title: f.name,
        subtitle: f.path,
        icon: getFileIcon(f.extension),
        action: 'openFile',
        actionArgs: { path: f.path }
      }))
      selectedIndex.value = 0
    } catch (error) {
      console.error('搜索失败:', error)
    }
    isLoading.value = false
  }

  function selectNext() {
    if (selectedIndex.value < results.value.length - 1) selectedIndex.value++
  }

  function selectPrev() {
    if (selectedIndex.value > 0) selectedIndex.value--
  }

  async function executeSelected() {
    const result = results.value[selectedIndex.value]
    if (!result) return

    if (result.type === 'file') {
      await window.mqbox.file.open(result.actionArgs.path)
      window.mqbox.window.hide()
    }
  }

  return { query, results, selectedIndex, isLoading, performSearch, selectNext, selectPrev, executeSelected }
})

function getFileIcon(ext: string): string {
  const iconMap: Record<string, string> = {
    doc: 'file-text', docx: 'file-text', pdf: 'file-text',
    xls: 'file-spreadsheet', xlsx: 'file-spreadsheet',
    ppt: 'presentation', pptx: 'presentation',
    jpg: 'image', jpeg: 'image', png: 'image', gif: 'image',
    mp3: 'music', mp4: 'video', avi: 'video',
    zip: 'archive', rar: 'archive', '7z': 'archive',
    exe: 'executable', msi: 'executable',
    js: 'code', ts: 'code', py: 'code', java: 'code'
  }
  return iconMap[ext.toLowerCase()] || 'file'
}
```

**Step 2: 创建 SearchBox 组件**

```vue
<script setup lang="ts">
import { ref, onMounted, onUnmounted, watch } from 'vue'
import { useSearchStore } from '../stores/search'
import { debounce } from 'lodash-es'

const store = useSearchStore()
const inputRef = ref<HTMLInputElement>()

const handleKeydown = (e: KeyboardEvent) => {
  if (e.key === 'ArrowDown') store.selectNext()
  else if (e.key === 'ArrowUp') store.selectPrev()
  else if (e.key === 'Enter') store.executeSelected()
  else if (e.key === 'Escape') window.mqbox.window.hide()
}

const debouncedSearch = debounce((q: string) => store.performSearch(q), 200)

watch(() => store.query, (q) => debouncedSearch(q))

onMounted(() => inputRef.value?.focus())
onUnmounted(() => debouncedSearch.cancel())
</script>

<template>
  <div class="search-box rounded-xl shadow-lg bg-[#FAFAFA] p-4 w-full">
    <!-- 搜索输入区 -->
    <div class="search-input flex items-center gap-3 bg-white rounded-lg px-3 py-2 border border-[#E0E0E0]">
      <icon-font name="search" class="text-[#666666]" size="20" />
      <input
        ref="inputRef"
        v-model="store.query"
        type="text"
        placeholder="搜索文件、命令、插件..."
        class="flex-1 outline-none text-sm text-[#1E1E1E]"
        @keydown="handleKeydown"
      />
      <icon-font name="x" class="text-[#666666] cursor-pointer" size="20" @click="window.mqbox.window.hide()" />
    </div>

    <!-- 结果列表 -->
    <div v-if="store.results.length > 0" class="result-list mt-2 flex flex-col gap-1">
      <div
        v-for="(result, index) in store.results"
        :key="index"
        class="result-item flex items-center gap-3 px-3 py-2 rounded-lg cursor-pointer transition-colors"
        :class="{ 'bg-[#E8F4FD]': index === store.selectedIndex, 'bg-white hover:bg-[#F5F5F5]': index !== store.selectedIndex }"
        @click="store.selectedIndex = index; store.executeSelected()"
      >
        <icon-font :name="result.icon" class="text-[#0078D4]" size="20" />
        <div class="flex-1">
          <div class="text-sm text-[#1E1E1E]">{{ result.title }}</div>
          <div class="text-xs text-[#999999] truncate">{{ result.subtitle }}</div>
        </div>
        <span class="text-xs text-[#999999]">Enter</span>
      </div>
    </div>

    <!-- 提示区 -->
    <div v-if="!store.query" class="hint-area mt-2 flex items-center gap-2 bg-[#F5F5F5] rounded px-3 py-2">
      <icon-font name="lightbulb" class="text-[#666666]" size="16" />
      <span class="text-xs text-[#666666]">输入 > 激活命令模式，输入 @插件名 直接调用插件</span>
    </div>
  </div>
</template>

<style scoped>
.search-box {
  max-height: 500px;
}

.result-list {
  max-height: 300px;
  overflow-y: auto;
}
</style>
```

**Step 3: Commit**

```bash
git add src/renderer/src/components/SearchBox.vue src/renderer/src/stores/search.ts
git commit -m "feat: 搜索框组件"
```

---

## Task 13: 插件管理器组件

**Files:**
- Create: `src/renderer/src/components/PluginManager.vue`
- Create: `src/renderer/src/stores/plugin.ts`

**Step 1: 创建插件状态 Store**

```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import type { PluginInfo } from '../../../shared/types'

export const usePluginStore = defineStore('plugin', () => {
  const plugins = ref<PluginInfo[]>([])
  const isLoading = ref(false)

  async function loadPlugins() {
    isLoading.value = true
    plugins.value = await window.mqbox.plugin.list()
    isLoading.value = false
  }

  async function enablePlugin(id: string) {
    await window.mqbox.plugin.enable(id)
    const plugin = plugins.value.find(p => p.id === id)
    if (plugin) plugin.enabled = true
  }

  async function disablePlugin(id: string) {
    await window.mqbox.plugin.disable(id)
    const plugin = plugins.value.find(p => p.id === id)
    if (plugin) plugin.enabled = false
  }

  return { plugins, isLoading, loadPlugins, enablePlugin, disablePlugin }
})
```

**Step 2: 创建 PluginManager 组件**

```vue
<script setup lang="ts">
import { onMounted } from 'vue'
import { usePluginStore } from '../stores/plugin'

const emit = defineEmits(['close', 'config'])
const store = usePluginStore()

onMounted(() => store.loadPlugins())

const handleInstall = async () => {
  // TODO: 实现插件安装
}

const openConfig = (id: string) => emit('config', id)
</script>

<template>
  <div class="plugin-manager rounded-xl shadow-lg bg-white p-5 w-[500px] h-[400px] flex flex-col">
    <!-- 标题栏 -->
    <div class="header flex-between mb-4">
      <span class="text-lg font-semibold text-[#1E1E1E]">插件管理</span>
      <div class="actions flex gap-3">
        <icon-font name="settings" class="text-[#666666] cursor-pointer" size="20" />
        <icon-font name="x" class="text-[#666666] cursor-pointer" size="20" @click="emit('close')" />
      </div>
    </div>

    <!-- 列表头部 -->
    <div class="list-header flex-between pb-2 border-b border-[#E0E0E0]">
      <span class="text-sm text-[#666666]">已安装插件 ({{ store.plugins.length }})</span>
      <button class="install-btn bg-[#0078D4] text-white text-xs px-3 py-1 rounded flex items-center gap-1" @click="handleInstall">
        <icon-font name="plus" size="14" />
        安装插件包
      </button>
    </div>

    <!-- 插件列表 -->
    <div class="plugin-list flex-1 flex flex-col gap-2 mt-2 overflow-y-auto">
      <div v-for="plugin in store.plugins" :key="plugin.id" class="plugin-card bg-[#F5F5F5] rounded-lg p-3">
        <div class="flex-between">
          <div class="flex items-center gap-2">
            <icon-font :name="plugin.icon || 'puzzle'" size="20" class="text-[#FFC107]" />
            <span class="text-sm font-medium text-[#1E1E1E]">{{ plugin.name }}</span>
            <span class="text-xs text-[#666666]">v{{ plugin.version }}</span>
          </div>
          <el-switch v-model="plugin.enabled" size="small" @change="(val: boolean) => val ? store.enablePlugin(plugin.id) : store.disablePlugin(plugin.id)" />
        </div>
        <div class="text-xs text-[#666666] mt-1">{{ plugin.description }}</div>
        <div class="flex justify-end mt-2">
          <button class="text-xs text-[#0078D4] hover:underline" @click="openConfig(plugin.id)">配置</button>
        </div>
      </div>
    </div>

    <!-- 底部提示 -->
    <div class="footer text-xs text-[#999999] text-center pt-2">
      从本地安装插件包 (.mqbox-plugin 或 .zip 格式)
    </div>
  </div>
</template>
```

**Step 3: Commit**

```bash
git add src/renderer/src/components/PluginManager.vue src/renderer/src/stores/plugin.ts
git commit -m "feat: 插件管理器组件"
```

---

## Task 14: 插件配置组件

**Files:**
- Create: `src/renderer/src/components/PluginConfig.vue`

**Step 1: 创建 PluginConfig 组件**

```vue
<script setup lang="ts">
import { ref, onMounted, watch } from 'vue'

const props = defineProps<{ pluginId: string }>()
const emit = defineEmits(['close'])

const config = ref<Record<string, any>>({})
const pluginInfo = ref<any>(null)

onMounted(async () => {
  pluginInfo.value = await window.mqbox.plugin.list().then(p => p.find(x => x.id === props.pluginId))
  config.value = await window.mqbox.config.get(`plugins.${props.pluginId}`) || {}
})

const saveConfig = async () => {
  await window.mqbox.config.set(`plugins.${props.pluginId}`, config.value)
  emit('close')
}
</script>

<template>
  <div class="plugin-config rounded-xl shadow-lg bg-white p-5 w-[420px] h-[380px] flex flex-col">
    <!-- 标题栏 -->
    <div class="header flex-between mb-4">
      <div class="flex items-center gap-2">
        <icon-font name="clipboard" size="20" class="text-[#FFC107]" />
        <span class="text-base font-semibold text-[#1E1E1E]">{{ pluginInfo?.name }} - 配置</span>
      </div>
      <div class="actions flex gap-3 items-center">
        <button class="bg-[#0078D4] text-white text-xs px-3 py-1 rounded" @click="saveConfig">保存</button>
        <icon-font name="x" class="text-[#666666] cursor-pointer" size="20" @click="emit('close')" />
      </div>
    </div>

    <!-- 配置内容 -->
    <div class="config-content flex-1 flex flex-col gap-5 overflow-y-auto">
      <!-- 历史记录数量 -->
      <div class="config-item flex flex-col gap-2">
        <label class="text-sm text-[#1E1E1E]">历史记录数量</label>
        <div class="flex items-center gap-2">
          <el-input v-model.number="config.maxHistory" type="number" size="small" class="w-[100px]" />
          <span class="text-xs text-[#666666]">条</span>
        </div>
      </div>

      <!-- 自动清理 -->
      <div class="config-item flex flex-col gap-2">
        <label class="text-sm text-[#1E1E1E]">自动清理</label>
        <el-checkbox v-model="config.autoCleanup">自动清理超过30天的记录</el-checkbox>
      </div>

      <!-- 排除应用 -->
      <div class="config-item flex flex-col gap-2">
        <label class="text-sm text-[#1E1E1E]">排除应用</label>
        <el-input v-model="config.excludeAppsStr" placeholder="密码管理器, 银行应用" size="small" />
      </div>

      <!-- 分隔线 -->
      <div class="border-t border-[#E0E0E0]" />

      <!-- 权限信息 -->
      <div class="permissions flex flex-col gap-2">
        <span class="text-sm font-medium text-[#1E1E1E]">权限信息</span>
        <div class="perm-list flex flex-col gap-1">
          <div v-for="perm in pluginInfo?.permissions" :key="perm" class="text-xs text-[#666666]">
            • {{ getPermLabel(perm) }}
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script lang="ts">
function getPermLabel(perm: string): string {
  const labels: Record<string, string> = {
    clipboard: '读取剪贴板',
    notification: '系统通知',
    'files:read': '读取文件',
    'files:write': '写入文件',
    httpRequest: '发送HTTP请求',
    shell: '执行系统命令'
  }
  return labels[perm] || perm
}
</script>
```

**Step 2: Commit**

```bash
git add src/renderer/src/components/PluginConfig.vue
git commit -m "feat: 插件配置组件"
```

---

## Task 15: 内置插件 - 剪贴板历史

**Files:**
- Create: `plugins/clipboard-history/package.json`
- Create: `plugins/clipboard-history/index.js`

**Step 1: 创建剪贴板插件 package.json**

```json
{
  "name": "clipboard-history",
  "version": "1.0.0",
  "displayName": "剪贴板历史",
  "description": "记录和管理剪贴板历史记录",
  "main": "index.js",
  "keywords": ["cb", "clipboard"],
  "activationEvents": ["onKeyword:cb", "onKeyword:clipboard"],
  "permissions": ["clipboard"],
  "config": {
    "maxHistory": { "type": "number", "title": "历史记录数量", "default": 100 },
    "autoCleanup": { "type": "boolean", "title": "自动清理", "default": true }
  }
}
```

**Step 2: 创建剪贴板插件 index.js**

```js
let history = []
let config = { maxHistory: 100, autoCleanup: true }

module.exports = {
  activate(context) {
    context.registerCommand('clipboard:show', async () => {
      return history.slice(0, 10).map(item => ({
        title: item.content.substring(0, 50),
        subtitle: item.time,
        action: 'clipboard:copy',
        actionArgs: { content: item.content }
      }))
    })

    context.registerSearchProvider({
      keyword: 'cb',
      onSearch: async (query) => {
        const filtered = history.filter(item => 
          item.content.toLowerCase().includes(query.toLowerCase())
        )
        return filtered.slice(0, 10).map(item => ({
          title: item.content.substring(0, 50),
          subtitle: new Date(item.time).toLocaleString(),
          action: 'clipboard:copy',
          actionArgs: { content: item.content }
        }))
      }
    })

    // 监听剪贴板变化
    setInterval(async () => {
      const text = await context.clipboard.readText()
      if (text && !history.some(h => h.content === text)) {
        history.unshift({ content: text, time: Date.now() })
        if (history.length > config.maxHistory) history.pop()
      }
    }, 500)
  },

  deactivate() {
    // 清理定时器
  }
}
```

**Step 3: Commit**

```bash
git add plugins/clipboard-history/package.json plugins/clipboard-history/index.js
git commit -m "feat: 剪贴板历史插件"
```

---

## Task 16: 内置插件 - 快速笔记

**Files:**
- Create: `plugins/quick-notes/package.json`
- Create: `plugins/quick-notes/index.js`

**Step 1: 创建笔记插件 package.json**

```json
{
  "name": "quick-notes",
  "version": "1.0.0",
  "displayName": "快速笔记",
  "description": "快速记录和管理笔记",
  "main": "index.js",
  "keywords": ["note", "笔记"],
  "activationEvents": ["onKeyword:note", "onCommand:note"],
  "permissions": ["files:read", "files:write"]
}
```

**Step 2: 创建笔记插件 index.js**

```js
let notes = []

module.exports = {
  activate(context) {
    context.registerCommand('note', async (args) => {
      const content = args.join(' ')
      const note = {
        id: Date.now().toString(),
        content,
        tags: extractTags(content),
        time: Date.now()
      }
      notes.unshift(note)
      await context.storage.set('notes', notes)
      return { title: '笔记已创建', subtitle: content.substring(0, 30) }
    })

    context.registerSearchProvider({
      keyword: 'note',
      onSearch: async (query) => {
        return notes
          .filter(n => n.content.includes(query) || n.tags.some(t => t.includes(query)))
          .slice(0, 10)
          .map(n => ({
            title: n.content.substring(0, 50),
            subtitle: n.tags.join(' ') + ' - ' + new Date(n.time).toLocaleDateString(),
            action: 'note:view',
            actionArgs: { id: n.id }
          }))
      }
    })
  },

  deactivate() {}
}

function extractTags(content) {
  const match = content.match(/#(\w+)/g)
  return match ? match.map(t => t.substring(1)) : []
}
```

**Step 3: Commit**

```bash
git add plugins/quick-notes/package.json plugins/quick-notes/index.js
git commit -m "feat: 快速笔记插件"
```

---

## Task 17: 内置插件 - 截图工具

**Files:**
- Create: `plugins/screenshot/package.json`
- Create: `plugins/screenshot/index.js`

**Step 1: 创建截图插件 package.json**

```json
{
  "name": "screenshot",
  "version": "1.0.0",
  "displayName": "截图工具",
  "description": "快速截图、标注、分享",
  "main": "index.js",
  "keywords": ["ss", "screenshot"],
  "activationEvents": ["onKeyword:ss", "onKeyword:screenshot"],
  "permissions": ["clipboard", "notification"]
}
```

**Step 2: 创建截图插件 index.js**

```js
module.exports = {
  activate(context) {
    context.registerCommand('screenshot', async () => {
      // 通过 IPC 调用主进程截图功能
      context.notification.show('截图工具', '请框选截图区域')
      return { action: 'screenshot:start' }
    })

    context.registerSearchProvider({
      keyword: 'ss',
      onSearch: async () => {
        return [
          { title: '区域截图', action: 'screenshot:region' },
          { title: '全屏截图', action: 'screenshot:fullscreen' },
          { title: '窗口截图', action: 'screenshot:window' }
        ]
      }
    })
  },

  deactivate() {}
}
```

**Step 3: Commit**

```bash
git add plugins/screenshot/package.json plugins/screenshot/index.js
git commit -m "feat: 截图工具插件"
```

---

## Task 18: 内置插件 - 计算器

**Files:**
- Create: `plugins/calculator/package.json`
- Create: `plugins/calculator/index.js`

**Step 1: 创建计算器插件 package.json**

```json
{
  "name": "calculator",
  "version": "1.0.0",
  "displayName": "计算器",
  "description": "基础数学运算和单位换算",
  "main": "index.js",
  "keywords": ["calc", "="],
  "activationEvents": ["onKeyword:calc", "onKeyword:="],
  "permissions": []
}
```

**Step 2: 创建计算器插件 index.js**

```js
module.exports = {
  activate(context) {
    context.registerCommand('calc', async (args) => {
      const expr = args.join('')
      try {
        const result = evaluate(expr)
        return { title: `= ${result}`, subtitle: expr }
      } catch (e) {
        return { title: '计算错误', subtitle: e.message }
      }
    })

    context.registerSearchProvider({
      keyword: '=',
      onSearch: async (query) => {
        try {
          const result = evaluate(query)
          return [{ title: `= ${result}`, subtitle: query, action: 'calc:copy', actionArgs: { result } }]
        } catch (e) {
          return []
        }
      }
    })
  },

  deactivate() {}
}

function evaluate(expr) {
  // 安全的数学表达式计算
  const sanitized = expr.replace(/[^0-9+\-*/().%\s]/g, '')
  return Function('"use strict"; return (' + sanitized + ')')()
}
```

**Step 3: Commit**

```bash
git add plugins/calculator/package.json plugins/calculator/index.js
git commit -m "feat: 计算器插件"
```

---

## Task 19: 资源文件

**Files:**
- Create: `resources/icon.png` (占位)
- Create: `resources/tray-icon.png` (占位)

**Step 1: 创建占位图标（可通过设计生成）**

暂时使用空白占位，后续替换为正式图标。

**Step 2: Commit**

```bash
git add resources/
git commit -m "feat: 资源文件占位"
```

---

## Task 20: 运行测试

**Step 1: 运行 TypeScript 类型检查**

Run: `npm run typecheck`
Expected: 无类型错误

**Step 2: 运行开发模式**

Run: `npm run dev`
Expected: 应用启动，搜索框显示

**Step 3: 测试快捷键**

操作: 按 Ctrl+Space
Expected: 搜索框显示/隐藏

**Step 4: 测试文件搜索**

操作: 输入关键词搜索
Expected: Everything返回结果（需开启Everything服务）

---

## 完成标志

所有功能实现完成，应用可正常运行。