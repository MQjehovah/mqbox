# 钉图功能 — 产品需求文档

## 需求概述

截图后可将图片"钉"在屏幕最上层（无边框窗口），支持拖拽移动和关闭删除，作为开发时的参考图浮窗使用。

---

## 用户故事

1. **作为前端开发者**，我可以在截图编辑器中点击"钉图"按钮，将注释后的截图以无边框窗口钉在屏幕最上层，以便编写代码时随时对照参考。

2. **作为任何需要参照的用户**，我可以用鼠标拖拽钉图窗口到屏幕任意位置，不遮挡我的工作区。

3. **作为用完参考图的用户**，我可以点击钉图窗口上的关闭按钮将其移除，释放屏幕空间。

4. **作为多任务用户**，我可以同时钉多张图在屏幕上（如改稿前后对比），每个窗口独立可拖动、可关闭。

---

## 功能优先级表

| 优先级 | 功能 | 说明 |
|--------|------|------|
| **P0** | 钉图窗口始终置顶（AlwaysOnTop） | 已有，需确认正常 |
| **P0** | 钉图窗口可拖拽移动 | **当前缺失**：`cursor:move` 设了但无拖拽逻辑 |
| **P0** | 钉图窗口有关闭按钮 | **当前缺失**：窗口无任何交互元素 |
| **P1** | 支持同时钉多张图 | 多个独立窗口并行存在 |
| **P1** | 钉图窗口可调整大小 | 用户可缩放窗口尺寸 |
| **P2** | 钉图窗口支持右键菜单 | 右键提供"关闭"、"保存"等操作 |
| **P2** | 快捷键关闭所有钉图 | `closeAllPins()` 已有，需绑定快捷键 |

---

## 六问质疑回答

### Q1：用户真正痛点是什么？
不是"需要钉图功能"，而是**"写代码时需要频繁Alt+Tab切换看图，注意力被打断"**。一次切换2-3秒，一天几十次，累积的是心流状态的中断。

### Q2：不做这个会怎样？
用户继续用"截图→保存桌面→图片查看器→Alt+Tab"的笨办法。已有替代方案，只是体验极差。不做不会死人，但每天都在损失效率。

### Q3：成功标准是什么？
- 用户截图后点击"钉图"，窗口立即出现在屏幕中心且置顶
- 用户能用鼠标拖拽移动窗口到任意位置
- 用户能点击关闭按钮消除窗口
- 整个流程无需额外学习成本

### Q4：技术上最大风险点？
**Electron 透明窗口拖拽**。BrowserWindow 的 `transparent: true` + `frame: false` 组合下，拖拽逻辑必须在渲染进程（注入的 HTML/JS）中实现，且需处理好 `mousedown` 在主进程中通过 `win.setPosition()` 移动窗口的边界情况。

### Q5：1天内交付砍什么？
砍掉：多钉图并行、调整大小、右键菜单。只留 **P0 三项：置顶 + 可拖拽 + 可关闭**。

### Q6：竞品对照
| 方案 | 优劣 |
|------|------|
| Windows 截图 + 照片查看器 | 免费，但不置顶，需Alt+Tab |
| Snipaste（截图/钉图神器） | 业界标杆，钉图/取色/回帖都做完了，F1截图F3钉图 |
| mqbox 钉图 | 优势在于**集成在截图工作流内**，截完→注释→钉图一条龙，不用开额外工具 |

**我们的差异化：** 不需要第三方软件。用户已经在用 mqbox 截图和注释，顺手一点就钉上去了，不需要切到 Snipaste。

---

## 范围边界

### 做
- ✅ 截图编辑器"钉图"按钮调通 `pinImage()` 流程
- ✅ 钉图窗口添加拖拽移动能力（鼠标拖拽窗口）
- ✅ 钉图窗口添加关闭按钮
- ✅ 已有功能确认：`alwaysOnTop`、`transparent`、`skipTaskbar`

### 不做（本次范围外）
- ❌ 不修改截图选区交互逻辑
- ❌ 不修改编辑器工具栏布局
- ❌ 不重新设计插件系统接口
- ❌ 不实现多钉图管理（并行多钉属于 P1，后续迭代）

---

## 验收标准

| # | 验收项 | 验证方式 |
|---|--------|----------|
| 1 | 截图后点击"钉图"按钮，关闭编辑器，桌面出现图片窗口 | 手动测试 |
| 2 | 钉图窗口始终置顶，不被其他窗口遮挡 | 手动测试 |
| 3 | 鼠标按住窗口任意位置可拖拽移动 | 手动测试 |
| 4 | 窗口右上角有关闭按钮，点击后窗口消失 | 手动测试 |
| 5 | 窗口无边框、无任务栏图标 | 手动测试 |
| 6 | 可以连续钉多张图，每张独立可拖/可关 | 手动测试 |
